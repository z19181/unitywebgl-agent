# Grafana Dashboard Provisioning — v0.3.3
apiVersion: 1

providers:
  - name: 'PartyGameSDK'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
